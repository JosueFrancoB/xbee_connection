version: '3'
services: 
    frames:
      privileged: true
      container_name: xbee
      build: .
      volumes:
        - /boot/firmware/:/app/config_host
networks:
  default:
    external:
      name: ac

